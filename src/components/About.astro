---
import { Plus } from "@lucide/astro";
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import { Icon } from 'astro-icon/components';

interface Props {
  title: string;
  photo?: ImageMetadata | string;
  // We'll change 'Content' to 'techStack' which is an array of objects
  techStack?: { name: string; iconName?: string }[];
  link?: string;
}

const { title, photo, techStack = [], link } = Astro.props;
---

<!-- <section id="about" class="py-20 px-4">
  <div class="max-w-4xl mx-auto">
    <div class="card card-side bg-base-200 shadow-sm">
      <div class="card-body items-center text-center">
        <h2 class="card-title text-2xl justify-center mb-6">{title}</h2>

        <div class="flex flex-wrap justify-center gap-6">
          {techStack.map((tech) => (
            <div class="flex flex-col items-center gap-2 group">
              <div class="p-4 bg-base-100 rounded-xl shadow-sm transition-transform group-hover:-translate-y-1">
                {tech.icon ? (
                  /* If using Lucide Icons */
                  <tech.icon class="size-8 text-primary" />
                ) : (
                  /* If using Image URLs (SVG/PNG) */
                  <img src={tech.logoUrl} alt={tech.name} class="size-8 object-contain" />
                )}
              </div>
              <span class="text-xs font-medium opacity-70">{tech.name}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section> -->

<section id="about" class="py-20 px-4">
  <div class="max-w-4xl mx-auto">
    <div class="card bg-base-200 shadow-sm">
      <div class="card-body items-center text-center">
        <h2 class="card-title text-2xl justify-center mb-8">{title}</h2>

        <div class="flex flex-wrap justify-center gap-8">
          {techStack.map((tech) => (
            <div class="flex flex-col items-center gap-2 group">
              <div class="p-3 bg-base-100 rounded-2xl shadow-sm transition-all duration-300 group-hover:scale-110 group-hover:shadow-md">
                <Icon name={tech.iconName} class="size-10" />
              </div>
              <span class="text-xs font-semibold opacity-60 group-hover:opacity-100">{tech.name}</span>
            </div>
          ))}
        </div>

        {/* ... Plus button code ... */}
      </div>
    </div>
  </div>
</section>